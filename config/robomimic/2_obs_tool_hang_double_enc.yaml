_target_: diffusion_policy.workspace.train_diffusion_unet_hybrid_workspace_no_env.TrainDiffusionUnetHybridWorkspaceNoEnv
seed: 42
checkpoint:
  save_last_ckpt: true
  save_last_snapshot: false
  topk:
    format_str: epoch={epoch:03d}-val_loss={val_loss:.4f}-val_ddim_mse={val_ddim_mse:.6f}.ckpt
    k: 5
    mode: min
    monitor_key: val_ddim_mse_0
dataloader:
  batch_size: 64
  num_workers: 8
  persistent_workers: true
  pin_memory: true
  shuffle: true
  prefetch_factor: 1
val_dataloader:
  batch_size: 64
  num_workers: 8
  persistent_workers: true
  pin_memory: true
  drop_last: true
  shuffle: true
  prefetch_factor: 1
ema:
  _target_: diffusion_policy.model.diffusion.ema_model.EMAModel
  inv_gamma: 1.0
  max_value: 0.9999
  min_value: 0.0
  power: 0.75
  update_after_step: 0
exp_name: defaults
logging:
  group: null
  id: null
  name: robomimic_tool_hang_2_obs_double_enc
  project: robomimic_tool_hang
multi_run:
  wandb_name_base: ${now:%Y.%m.%d-%H.%M.%S}_train_robomimic_tool_hang_2_obs_double_enc
  run_dir: data/outputs/${multi_run.wandb_name_base}
horizon: ${eval:${n_obs_steps} + 14}
n_action_steps: 8
n_obs_steps: 2
shape_meta:
  action:
    shape:
    - 7
  obs:
    sideview_image:
      shape:
      - 240
      - 240
      - 3
      type: rgb
    robot0_eye_in_hand_image:
      shape:
      - 240
      - 240
      - 3
      type: rgb
    robot0_eef_pos:
      shape:
      - 3
      type: low_dim
    robot0_eef_quat:
      shape:
      - 4
      type: low_dim
    robot0_gripper_qpos:
      shape:
      - 2
      type: low_dim
policy:
  _target_: diffusion_policy.policy.diffusion_unet_hybrid_image_attention_policy.DiffusionUnetHybridImageAttentionPolicy
  diffusion_step_embed_dim: 128
  down_dims:
  - 256
  - 512
  - 1024
  kernel_size: 5
  n_groups: 8
  horizon: ${horizon}
  n_action_steps: ${n_action_steps}
  n_obs_steps: ${n_obs_steps}
  past_action_visible: false
  num_attention_heads: 8
  attention_dropout: 0.1
  use_temporal_pos_emb: true
  use_modality_emb: true
  use_range_emb: true
  DDPM_noise_scheduler:
    _target_: diffusers.schedulers.scheduling_ddpm.DDPMScheduler
    beta_end: 0.02
    beta_schedule: squaredcos_cap_v2
    beta_start: 0.0001
    clip_sample: true
    num_train_timesteps: 100
    prediction_type: epsilon
    variance_type: fixed_small
  num_DDPM_inference_steps: 100
  num_DDIM_inference_steps: 10
  shape_meta: ${shape_meta}
  inference_loading: false
  short_range_obs_horizon: 2
  short_range_dropout: 0.3
  obs_encoder:
    _target_: diffusion_policy.model.vision.robomimic_config_util.RobomimicObsEncoder
    shape_meta: ${shape_meta}
    pretrained_encoder: true
    freeze_encoder: false
    crop_shape: null
  short_range_encoder:
    _target_: diffusion_policy.model.vision.robomimic_config_util.RobomimicObsEncoder
    shape_meta: ${shape_meta}
    pretrained_encoder: true
    freeze_encoder: false
    crop_shape: null
task:
  dataset:
    _target_: diffusion_policy.dataset.robomimic_image_dataset.RobomimicImageDataset
    dataset_path: data/diffusion_experiments/robomimic/tool_hang/ph/image_v15.hdf5
    shape_meta: ${shape_meta}
    horizon: ${horizon}
    n_obs_steps: ${n_obs_steps}
    pad_before: ${eval:${n_obs_steps} - 1}
    pad_after: ${eval:${horizon} - 1}
    seed: ${seed}
    val_ratio: 0.05
    max_train_episodes: null
  name: robomimic_tool_hang
  shape_meta: ${shape_meta}
task_name: robomimic_tool_hang
training:
  mixed_precision: bf16
  gradient_clip_val: 1.0
  use_torch_compile: false
  torch_compile_mode: default
  checkpoint_path: null
  checkpoint_every: 5
  debug: false
  device: cuda
  gradient_accumulate_every: 1
  lr_scheduler: cosine
  lr_warmup_steps: 500
  encoder_lr_scale: 1.0
  max_train_steps: null
  max_val_steps: null
  total_train_steps: 200000
  num_epochs: null
  resume: false
  rollout_every: 5
  sample_every: ${training.checkpoint_every}
  seed: ${seed}
  tqdm_interval_sec: 30.0
  use_ema: true
  val_every: 1
  log_val_mse: true
  eval_mse_DDPM: true
  eval_mse_DDIM: true
optimizer:
  _target_: torch.optim.AdamW
  betas:
  - 0.95
  - 0.999
  eps: 1.0e-08
  lr: 0.0001
  weight_decay: 1.0e-06
